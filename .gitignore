# 1. 일단 최상위 경로의 모든 파일과 폴더를 업로드 대상에서 제외합니다.
/*

# 2. 명시적으로 깃허브에 올릴 폴더와 필수 파일만 예외로 허용(!)합니다.
!/src/
!/data/
!.gitignore
!README.md
!LICENSE
!requirements.txt

# 3. 허용된 폴더(src, data) 내부라 하더라도, 아래 확장자들은 절대 올리지 않습니다.
*.db
*.json
*.xlsx
*.pdf
*.txt
*.log
*.pkl
__pycache__/